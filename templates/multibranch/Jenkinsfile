pipeline {
    agent any

    properties([parameters([string(defaultValue: 'wololo', description: 'wololo', name: 'wololo', trim: true)]), [$class: 'JobLocalConfiguration', changeReasonComment: '']])
    
    stages {
        stage('Checkout') { 
            steps {
                script {
                    properties([parameters([booleanParam('test')])])
                }
                git credentialsId: "${github_creds}" , url: "${github_repo}" 
            }
        }
    }
  }
